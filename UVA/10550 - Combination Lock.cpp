#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a,b,c,d;
    cin>>a>>b>>c>>d;
    int sum=0,
    sum+=40;
    if(a>20) mida=a-20;
    else mida=a+20;
    if(b>=mida) sum+=(20-abs(b-a));
    else sum+=
}
